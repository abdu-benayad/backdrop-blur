# Build artifacts
/target
**/target

# The winit example is a separate workspace; ignore its lockfile churn is NOT desired —
# keep its Cargo.lock tracked for reproducible example builds. Only target/ is ignored.

# Generated example output — PNGs are regenerated by `cargo run` in each example.
# The example source is the committed artifact; the rendered images are not.
/examples/frost-gallery/out/
/examples/_shots/

# Editor / OS noise
*.swp
.DS_Store
